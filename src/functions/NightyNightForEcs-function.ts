// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for NightyNightForEcsFunction
 */
export interface NightyNightForEcsFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/functions/NightyNightForEcs.
 */
export class NightyNightForEcsFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: NightyNightForEcsFunctionProps) {
    super(scope, id, {
      description: 'src/functions/NightyNightForEcs.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/functions/NightyNightForEcs.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}